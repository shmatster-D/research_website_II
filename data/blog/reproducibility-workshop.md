Spent two days at a workshop dedicated entirely to the problem of getting code from *one* machine to run on *another* machine. Some recurring themes:

- Containers help, but only if someone keeps the Dockerfile honest
- A pinned dependency list beats a friendly README every time
- The most reproducible papers had the smallest, most boring codebases

I came away convinced that reproducibility is mostly a discipline problem, not a tooling problem.
